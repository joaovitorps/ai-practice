#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../handlebars/bin/handlebars" "$@"
else
  exec node  "$basedir/../../../handlebars/bin/handlebars" "$@"
fi
# cmd-shim-target=/home/joao/Projects/rocketseat/ai-practice/node_modules/.pnpm/eslint-plugin-boundaries@6.0.2_@typescript-eslint+parser@8.59.3_eslint@10.4.0_jiti@2.7._e6ad193fe5f0cca20ced86ea8cb6982d/node_modules/handlebars/bin/handlebars
