#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../typescript/bin/tsserver" "$@"
else
  exec node  "$basedir/../../../typescript/bin/tsserver" "$@"
fi
# cmd-shim-target=/home/joao/Projects/rocketseat/ai-practice/node_modules/.pnpm/react-i18next@17.0.8_i18next@26.2.0_typescript@6.0.3__react-dom@19.2.6_react@19.2.6__react@19.2.6_typescript@6.0.3/node_modules/typescript/bin/tsserver
